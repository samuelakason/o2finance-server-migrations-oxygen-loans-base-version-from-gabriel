import 'package:flutter/material.dart';

class LoanBox extends StatelessWidget {
  const LoanBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}